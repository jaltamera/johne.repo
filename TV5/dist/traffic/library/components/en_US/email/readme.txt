
Description

email.opal is a network that interprets email addresses. Email addresses are matched according the regular expression:

([a-zA-Z])([:alnum:]|_|\.)*@([:alnum:])+(\.[:alnum:]+)+

The output format is XML.


Integration Notes


Known Issues


Custom Agents

com.dejima.library.regularexpression.RegularExpressionAgent
